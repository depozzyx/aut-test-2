export type TStepperProps = {
  /**
   * List of steps to show.
   *
   * Each step is a string and will be represena as label.
   *
   * @type string[]
   */
  steps: string[]
  /**
   * Active step number.
   * @type number
   */
  activeStep: number
}
