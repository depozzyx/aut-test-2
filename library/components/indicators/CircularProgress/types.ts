export type TCircularProgressProps = {
  /**
   * Define border width of the loader in pixels number.
   */
  borderWidth: number
  /**
   * The progress in numbers. From 0 to 100.
   *
   * @default 0
   */
  progress: number
  /**
   * The size in pixels of the loader.
   *
   * @default 40
   */
  size?: number
}
