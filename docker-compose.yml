services:
  autodialer-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3001:3001'
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - ./apps/main/.env
    command: 'yarn start-main'
